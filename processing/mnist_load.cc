#include <stdio.h>
#include <stdlib.h>
#include <iostream>

// heavily inspired by "canonical" mnist data readers

using namespace std;

int main(){

  //FILE* f;
  //f = fopen("../data/mnist/test.txt","rb");

  if (f == NULL) printf("FAIL!\n");
  else printf("File opened\n");

  string filename = "a";

  //vector<vector<double>> vec;

  //mnist_load(filename,vec);

  cout << filename << "\n";

  return 0;
}
